# ================================================================
# Global & Backend Configuration
# ================================================================
PROJECT_NAME="Land Record Digitization Platform"
ENVIRONMENT="development"
DEBUG=True
API_V1_STR="/api/v1"

# CORS Origins (comma-separated or JSON list)
BACKEND_CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000,http://127.0.0.1:8000"

# ================================================================
# Security & JWT Authentication
# ================================================================
SECRET_KEY="super_secret_jwt_key_for_land_record_platform_change_in_production"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ================================================================
# Database Configuration (PostgreSQL with SQLite fallback)
# ================================================================
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_password
POSTGRES_DB=doc_platform

# Full Database URL (leave unset or comment out to auto-assemble or use SQLite fallback)
# DATABASE_URL="postgresql+psycopg://postgres:postgres_password@localhost:5432/doc_platform"
# For local standalone development with SQLite:
# DATABASE_URL="sqlite:///./doc_platform.db"

# ================================================================
# MinIO Object Storage Configuration
# ================================================================
MINIO_ENDPOINT="localhost:9000"
MINIO_ROOT_USER="minioadmin"
MINIO_ROOT_PASSWORD="minioadmin_password"
MINIO_BUCKET_NAME="documents"
MINIO_SECURE=False

# ================================================================
# Redis & Celery Background Tasks
# ================================================================
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_DB=0
# CELERY_BROKER_URL="redis://localhost:6379/0"
# CELERY_RESULT_BACKEND="redis://localhost:6379/0"

# ================================================================
# Multimodal OCR & Handwriting Model Configuration
# ================================================================
# Local TrOCR directory containing model checkpoints (configurable local path or relative)
TROCR_MODEL_DIR="C:/Users/akars/OneDrive/Documents/Land_Record_Models_FULL/models/trocr"

# Path to fine-tuned TrOCR Kannada model checkpoint directory
KANNADA_HANDWRITING_MODEL_PATH="models/trocr/kannada_generalized_v2_checkpoints/best_checkpoint"

# General model checkpoint directory
MODEL_CHECKPOINT_DIR="C:/Users/akars/OneDrive/Documents/Land_Record_Models_FULL/models/trocr"

# Target device for OCR inference (auto, cuda, cpu)
DEFAULT_DEVICE="auto"
OCR_DEVICE="auto"

# Default OCR language (kannada, english, telugu, tamil, hindi)
DEFAULT_OCR_LANGUAGE="kannada"
OCR_CONFIDENCE_THRESHOLD=0.60

# ================================================================
# OCR Data Directories & Training Paths
# ================================================================
RAW_DATA_DIR="./data/raw"
PROCESSED_DATA_DIR="./data/processed"
ANNOTATIONS_DIR="./data/annotations"

# ================================================================
# OCR Training Hyperparameters
# ================================================================
BATCH_SIZE=8
NUM_EPOCHS=10
LEARNING_RATE=5e-5

# ================================================================
# Semantic Layer (AI-Driven Information Extraction)
# ================================================================
SEMANTIC_ENGINE="gemini"  # "gemini", "local", "rule_fallback"
GEMINI_API_KEY=""
SEMANTIC_MODEL_NAME="gemini-3.1-flash-lite"
SEMANTIC_TIMEOUT_SECONDS=30

SEMANTIC_TEMPERATURE=0.0

# ================================================================
# Frontend Environment (for frontend/.env.local)
# ================================================================
NEXT_PUBLIC_API_URL="http://localhost:8000"