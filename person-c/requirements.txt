# Core & Data Contracts
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Image Processing & Preprocessing (Person A)
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# OCR Engines (Person A) — REQUIRED, was missing entirely before, which
# was the root cause of OCR always failing and falling back to fake data
easyocr>=1.7.2
pytesseract>=0.3.10
# NOTE: pytesseract also requires the system Tesseract binary + language
# packs installed separately (not a pip package):
#   Ubuntu/Debian: apt-get install tesseract-ocr tesseract-ocr-tam \
#                  tesseract-ocr-kan tesseract-ocr-hin tesseract-ocr-mar
#   Windows: install from https://github.com/UB-Mannheim/tesseract/wiki

# Spatial & GIS (Person C)
shapely>=2.0.0
geopandas>=0.14.0
pyproj>=3.6.0

# Database & Vectors (Person C)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pgvector>=0.2.5

# Machine Learning & Metrics
scikit-learn>=1.4.0
scipy>=1.11.0

# MLflow & Experiment Tracking (Person B)
mlflow>=2.12.0

# Logging & Configuration
python-dotenv>=1.0.0
loguru>=0.7.2

# Testing & Evaluation
pytest>=8.0.0
pytest-cov>=4.1.0
httpx2>=0.1.0
fastapi>=0.110.0